import { Badge } from "@material-ui/core";
// import "./card.css";

const Card = ({ id, poster, title, date, media_type, vote_average }) => {
  return <div>{id}</div>;
};

export default Card;
